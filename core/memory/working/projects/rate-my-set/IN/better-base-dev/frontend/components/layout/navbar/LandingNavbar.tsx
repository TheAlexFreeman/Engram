import { Outlet } from '@tanstack/react-router';

function LandingNavbar() {
  return (
    <div>
      Not yet implemented with new styling solution.
      <Outlet />
    </div>
  );
}

export default LandingNavbar;
