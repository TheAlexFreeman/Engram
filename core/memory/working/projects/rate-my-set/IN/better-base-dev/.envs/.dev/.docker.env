# General
# ------------------------------------------------------------------------------
USE_DOCKER=True
IPYTHONDIR=/app/.ipython

# `dev_env_files.py` mixin doesn't need to be enabled when we're running within Docker.
# ------------------------------------------------------------------------------
DJANGO_READ_DEV_ENV_FILES=False
DJANGO_READ_ENV_LOCAL_FILE=False
